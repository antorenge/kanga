<nav class="pagination">
	{{#if prev}}
		<a class="previous" href="{{page_url prev}}">←</a>
	{{/if}}
	<span class="page-number">Page {{page}} of {{pages}}</span>
	{{#if next}}
	<a class="next" href="{{page_url next}}">→</a>
	{{/if}}
</nav>